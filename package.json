{
  "name": "style-dictionary-example-complete",
  "version": "1.0.0",
  "description": "",
  "main": "build.js",
  "scripts": {
    "build:example": "node_modules/.bin/style-dictionary build",
    "build": "style-dictionary build",
    "clean": "rm -rf build",
    "clean:windows": "rmdir build /S /Q",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "devDependencies": {
    "style-dictionary": "latest"
  }
}